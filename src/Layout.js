import React from 'react';
import Header from "./Header";
import LeftBar from "./LeftBar";

const Layout = () => {
    return (
        <div>
            <Header/>
            <LeftBar/>
        </div>
    );
};

export default Layout;